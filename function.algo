procedure insertionSort(arr: array of integer)
    n := length(arr)
    for i from 0 to n - 1 do
        key := arr[i]
        j := i - 1
        while (j >= 0 and arr[j] > key) do
            arr[j + 1] := arr[j]
            j := j - 1
        arr[j + 1] := key
end procedure
